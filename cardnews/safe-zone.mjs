// safe-zone.mjs — 인스타 릴스 화면에서 **앱 UI 가 덮는 자리**의 단일 출처. 2026-09-18 신설.
//
// ⛔⛔ 왜 만들었나 — **같은 사실이 세 곳에 있었고 세 곳 다 틀렸다.**
//   `make-termcast.mjs:334` bottom:300px · `make-char-plate.mjs:40` bottom:200 ·
//   `overlay-lines.mjs:104` 하단 20%(=384@1080). 셋이 **서로 다른 숫자**였고,
//   `log.md:14208` 이 *"우리 300 도 근거가 기록에 없다"* 고 적어둔 채 **미완**으로 남아 있었다.
//   2026-09-18 에 실측해서 닫았다. ▶ 이제 숫자는 **이 파일에만** 있다.
//
// ── 🔬 측정 (2026-09-18) ────────────────────────────────────
//   사용자가 인스타 앱에서 발행 릴스를 열고 스크린샷 2장(1080×2340 · 같은 회차 · 09:21).
//   눈금: 스팅의 초록 알약 띠를 찾고(y 1888~1994), `make-brand-sting.mjs` 의
//         `.cta { bottom:0; padding: 48px 40px 60px }` 로 **프레임 바닥 = 알약바닥 + 60 = 2054** 를 세웠다.
//   ✅ 검산 둘 — 알약 높이 **실측 107px vs CSS 계산 104px** 이라 영상이 **1080×1920 을 1:1** 로 그린다.
//      DHE 마크 중심 1058 ≈ 프레임 중심 1094 에서 `.rule` 만큼 위. 맞는다.
//   ⛔ 1차 탐지기는 **DHE 글자를 알약으로 잡았다**(둘 다 accent 색). 프레임 꼭대기가 −732 로 나와서 잡혔다.
//
// ── ⚠️ 이 측정이 못 말하는 것 ───────────────────────────────
//   · **소유자 화면**이다. 「인사이트 보기 / 릴스 홍보하기」 줄은 남에게 안 보인다.
//     ▶ 다만 그 줄은 캡션 **아래**이고 버튼 열은 **모두에게 보인다** → 650 은 **안전한 쪽**이다.
//   · **기기 1대 · 앱 1버전 · `n=1`** 이다. 기기마다 다를 수 있다.
//   · **상단은 안 쟀다.** 아래 `상단nav` 는 기존 8% 를 그대로 둔 것이고 **측정값이 아니다.**

/** 🔬 실측값. 1080×1920 프레임 기준 픽셀. ⛔ 여기 말고 다른 데 적지 않는다. */
export const 측정 = {
  날짜: '2026-09-18',
  기기: 'Android 1080×2340 · 인스타 앱 · 소유자 화면',
  기준: { w: 1080, h: 1920 },
  /** 프로필 아바타·캡션 블록의 위쪽 끝이 프레임 바닥에서 얼마나 위인가 */
  하단UI: 650,
  /** 하트·댓글·공유·저장·더보기 열이 오른쪽 가장자리에서 차지하는 폭 */
  우측버튼열: 320,
  /** ⚠️ 측정 안 함 — 기존 8% 를 그대로 둔 값이다 */
  상단nav: Math.round(1920 * 0.08),
  /** 소유자에게만 보이는 구분선(참고용, 규칙에 안 쓴다) */
  참고_소유자구분선: 421,
};

/** 옛 후보들 — 지우지 않는다. 이력이 근거다. 전부 실측보다 **헐거웠다.** */
export const 기각된후보 = {
  '우리 코드 300 (make-termcast:334 · README)': 300,
  '우리 코드 384 (overlay-lines 하단 20%)': 384,
  '수집 출처 B 480': 480,
  '수집 출처 C 192~288': 288,
};

/**
 * 구도값 — **측정이 아니라 선택이다.**
 * ⛔ `캐릭터바닥` 은 측정(650)보다 **일부러 낮다.** 캐릭터는 장식이라 부분 가림이 치명적이 아니고,
 *    이미 발행된 회차들이 이 구도로 나갔다. 여기서 올리면 **옛 회차와 구도가 갈린다.**
 *    ▶ 바꾸려면 사용자 결정이 필요하다. 그때 이 한 줄만 고치면 두 도구가 같이 따라온다.
 * 🔬 현재 구도의 실제 상태(2026-09-18): `make-termcast` 레이아웃이 캐릭터를 920~1620 에 두는데
 *    실측 임계는 1270 이라 **캐릭터 하단 약 350px 이 캡션 아래 깔린다.** 자막(640~894)은 안전하다.
 */
export const 구도 = {
  캐릭터바닥: 300,
  캐릭터우측여백: 56,
};

/** 1080 기준 값을 다른 폭으로 환산한다. (720 → ×2/3) */
export const 환산 = (값, 폭) => Math.round((값 * 폭) / 측정.기준.w);

/**
 * **글자·자막**이 지켜야 하는 경계. 구도값과 달리 이건 타협하지 않는다 —
 * 글자가 UI 아래 깔리면 그 회차는 읽을 수 없다.
 * @param {number} w 프레임 폭 (예: 720)
 * @param {number} h 프레임 높이 (예: 1280)
 */
export function 글자안전(w, h) {
  return {
    /** y 가 이보다 작으면 상단 nav 와 겹친다 */
    상단: 환산(측정.상단nav, w),
    /** 블록 바닥이 이보다 크면 캡션·프로필과 겹친다 */
    하단임계: h - 환산(측정.하단UI, w),
    /** 블록 오른쪽 끝이 이보다 크면 버튼 열과 겹친다 */
    우측임계: w - 환산(측정.우측버튼열, w),
  };
}

/**
 * 글자 블록 하나를 검사한다. 경고 문자열 배열을 돌려준다(비어 있으면 안전).
 * ⚠️ `우측끝` 은 부르는 쪽이 계산해서 넘긴다 — 폰트 폭 근사는 도구마다 다르다.
 */
export function 검사글자({ y, 바닥, 우측끝, w, h }) {
  const s = 글자안전(w, h);
  const warns = [];
  if (y < s.상단) warns.push(`상단 ${y}px 가 상단 nav 구역(${s.상단}px) 안이다 ⚠️ 이 값은 미측정이다`);
  if (바닥 > s.하단임계) {
    warns.push(`바닥 ${Math.round(바닥)}px 가 캡션·프로필 구역(임계 ${s.하단임계}px) 안이다`
      + ` — 실측 ${측정.하단UI}px@1080, ${측정.날짜}`);
  }
  if (우측끝 > s.우측임계) {
    warns.push(`오른쪽 끝 ${Math.round(우측끝)}px 가 버튼 열(임계 ${s.우측임계}px) 안이다`
      + ` — 실측 ${측정.우측버튼열}px@1080, ${측정.날짜}`);
  }
  return warns;
}
